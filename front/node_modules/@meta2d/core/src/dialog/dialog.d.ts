import { Meta2d } from "../core";
import { Meta2dStore } from '../store';
export declare class Dialog {
    parentElement: HTMLElement;
    box: HTMLElement;
    iframe: HTMLIFrameElement;
    dialog: HTMLElement;
    close: HTMLElement;
    title: HTMLElement;
    body: HTMLElement;
    x: number;
    y: number;
    url: string;
    meta2dDiv: HTMLElement;
    dialogMeta2d: Meta2d;
    store: Meta2dStore;
    constructor(parentElement: HTMLElement, store: Meta2dStore);
    show(title?: string, url?: string, rect?: {
        x: number;
        y: number;
        width: number;
        height: number;
    }, data?: any): Promise<void>;
    hide(): void;
    isUrl(url: string): boolean;
    destroy(): void;
}
